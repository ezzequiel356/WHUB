# Para desarrollo LOCAL (node --watch server.js, sin Docker).
# Copiar a ".env" en esta misma carpeta y completar. Corriendo con
# docker compose, estas variables las inyecta docker-compose.yml y no
# hace falta este archivo dentro del contenedor.

DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=warframe
